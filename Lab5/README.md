In this lab, we:

* Created a new engineered feature to improve prediction of `Order_Status`
* Modified the `is_peak_hour` rule and evaluated performance changes
* Tested different `top_k` values and compared accuracy
* Analyzed top feature importances
* Applied optional feature selection and evaluated its impact on performance
