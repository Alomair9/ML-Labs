## Lab 9 Summary

In this lab, we:

* Explored the loan dataset using visualizations
* Analyzed relationships between features and the target variable `not.fully.paid`
* Converted categorical features into numerical format using one-hot encoding
* Split the data into training and testing sets
* Trained a Decision Tree classifier
* Evaluated the model using classification metrics
* Trained a Random Forest model and compared its performance with the Decision Tree

