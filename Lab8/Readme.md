## Lab 8 Summary

In this lab, we:

* Explored the dataset using seaborn visualizations
* Standardized the features using StandardScaler
* Split the data into training and testing sets
* Trained a K-Nearest Neighbors (KNN) model
* Evaluated the model using classification metrics
* Used the elbow method to find the optimal K value
* Retrained the model with the best K and compared performance

